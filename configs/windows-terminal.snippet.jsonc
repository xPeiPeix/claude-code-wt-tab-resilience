// Windows Terminal settings.json — 两处插入示例
//
// 路径：%LOCALAPPDATA%\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState\settings.json
// 不要直接覆盖整个文件，只把下面两处合并到自己的现有配置。
//
// 改完保存后 WT 会自动 reload；firstWindowPreference 在下次启动 WT 时生效。
//
// 注意：v0.1.0 曾推荐的 profiles.defaults.bellStyle 配置已在 v0.2.0 移除。
// 原因：Claude Code hook 子进程的 stdout/stderr 全部被捕获到 debug log，
// 且子进程无 controlling tty，没有任何路径让 BEL 字符到达 terminal 触发
// WT bellStyle 视觉响铃。改用 PowerShell + BurntToast 调系统 toast API 实现通知。
{
  // ==================================================================
  // 1) 顶层新增 — 关 WT 时自动保存所有窗口/tab/分屏布局，下次启动自动还原
  // ==================================================================
  "firstWindowPreference": "persistedWindowLayout",

  // ==================================================================
  // 2) keybindings 数组追加 — Ctrl+Shift+Z 撤销关闭 tab/pane
  //    栈深 100 个，可连按多次撤销，完整保留分屏方向、比例、profile、CWD
  //    （不恢复跑着的进程，需要重启对应命令）
  // ==================================================================
  "keybindings": [
    {
      "id": "Terminal.RestoreLastClosed",
      "keys": "ctrl+shift+z"
    }
  ]
}
